# Migration `000004` — auth columns on `users`

- **Up:** adds `password_hash` (bcrypt, nullable so pre-auth rows keep working — they just can't log in), `email_verified` (default false), `verification_token`, `reset_token`, `reset_expires_at`, plus indexes on both token columns.
- **Down:** drops the indexes and all five columns.
