# Migration `000005` — OTP columns on `users`

- **Up:** adds `otp_hash` (SHA-256 of the 6-digit code — the plain code is never stored), `otp_expires_at` (10 min), `otp_attempts` (default 0, capped at 5 per code).
- **Down:** drops all three columns.
